import React from 'react';
import ReactLoading from 'react-loading';

const Loading = ({ type, color }) => (
    <ReactLoading type={"spokes"} color={"#686969"} height={'5rem'} width={'5rem'} />

);

export default Loading;
